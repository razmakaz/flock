// import type { Cookies } from '@sveltejs/kit';
// import { decodeJWT, signJWT } from './jwt';
// import god from './god';
// import type { ISession } from '$lib/@types/ISession';

// const sessionFactory = (userId: string): ISession => {
//     return {
//         userId,
//         id: string;
//         userId: string;
//         email: string;
//         name: string;
//         role: ''
//         iat: Math.floor(Date.now() / 1000),
//         exp: Math.floor(Date.now() / 1000) + 60 * 60 * 24 * 30 // 30 days
//     };
// }

// const generateSession = (userId) => {

// }
